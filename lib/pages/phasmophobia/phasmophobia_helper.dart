import 'package:flutter/material.dart';

class PhasmophobiaHelper extends StatelessWidget {
  const PhasmophobiaHelper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Scaffold(
    body: Placeholder(),
  );
}
